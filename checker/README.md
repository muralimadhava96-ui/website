# Checker Folder

This directory is reserved for the broken link checker implementation.

The project already includes a script under `scripts/checkLinks.js` and an npm script `check:links` to scan markdown files for broken links. Any future utilities or configurations related to link checking can live here.